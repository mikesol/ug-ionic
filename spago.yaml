workspace:
  packageSet:
    registry: 31.0.0
  extraPackages:
    hyrule:
      repo: "https://github.com/mikesol/purescript-hyrule.git"
      version: "v2.5.0"
      dependencies:
        - effect
        - js-timers
        - web-html
        - free
        - random
        - avar
        - filterable
        - unsafe-reference
        - web-uievents
    deku:
      git: "https://github.com/mikesol/purescript-deku.git"
      subdir: deku
      ref: "45479f8"
      dependencies:
        - hyrule
        - untagged-union
    deku-core:
      git: "https://github.com/mikesol/purescript-deku.git"
      subdir: deku-core
      ref: "45479f8"
      dependencies:
        - hyrule
        - untagged-union
        - record
    deku-dom:
      git: "https://github.com/mikesol/purescript-deku.git"
      subdir: deku-dom
      ref: "45479f8"
      dependencies:
        - hyrule
        - untagged-union
        - web-pointerevents
        - web-touchevents
    deku-ionic:
      git: "https://github.com/mikesol/purescript-deku-ionic.git"
      subdir: deku-ionic
      ref: "a0bab4d"
